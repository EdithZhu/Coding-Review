import java.util.*;
public class Solution {
    public static void main(String arg[]) {
        String str1 = "abcde";
        String str2 = "deabc";
        if ((str1.length() == str2.length()) && (str1 + str1).indexOf(str2) != -1)
        System.out.println("Two Strings are rotations of each other: Yes");
        else 
        System.out.println("Two Strings are rotations of each other: No");
    }
}
//Two Strings are rotations of each other: Yes
